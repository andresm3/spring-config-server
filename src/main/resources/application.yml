server:
  port: 7000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/andresm3/spring-config-server.git
          search-paths: /config-git